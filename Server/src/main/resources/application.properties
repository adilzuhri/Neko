# spring.servlet.multipart.enabled=true
# spring.datasource.url=jdbc:mysql://localhost:3306/nekodb
# spring.datasource.username=admin
# spring.datasource.password=${password}
# spring.datasource.hikari.connectionTimeout=20000
# spring.datasource.hikari.maximumPoolSize=5

spring.servlet.multipart.enabled=true
spring.datasource.url=jdbc:mysql://nekodb-mysql-90942-do-user-13626074-0.b.db.ondigitalocean.com:25060/defaultdb?ssl-mode=REQUIRED
spring.datasource.username=doadmin
spring.datasource.password=${password}
spring.datasource.hikari.connectionTimeout=20000
spring.datasource.hikari.maximumPoolSize=5

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${email.from}
spring.mail.password=${email.password}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# accesskey=${accessKey}
# secretkey=${secretKey}
# endpoint=${endpoint}
# endpoint.region=${endpointRegion}

jwt.secret=nekokona

# do.storage.key=${DO_STORAGE_KEY}
# do.storage.secretkey=${DO_STORAGE_SECRETKEY}
# do.storage.endpoint=${DO_STORAGE_ENDPOINT}
# do.storage.endpoint.region=${DO_STORAGE_ENDPOINT_REGION}