package nus.iss.Neko.Server.services;

import org.springframework.http.HttpEntity;
import org.springframework.http.HttpMethod;

public enum RestTemplate {
    ;

    public static void exchange(String url, HttpMethod put, HttpEntity<?> requestEntity, Class<Void> class1,
            String cat_id) {
    }

}
